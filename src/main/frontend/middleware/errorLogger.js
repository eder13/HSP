// TODO: Session expired listener
// if last HTTP Status Code is 401 and selectUserName.length > 0 ->
// logged in and also means session expired
// last HTTP Status Code with RTK Query
// see: https://redux-toolkit.js.org/rtk-query/usage/error-handling#handling-errors-at-a-macro-level
